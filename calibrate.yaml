path: /root/autodl-tmp/calibration_data_100  # 数据集根目录
train: images  # 关键：补充此键，使其与val指向同一路径
val: images    # 校准实际使用的图片路径

# 注意：校准过程本身不需要以下字段，但如果库检查需要也可以保留
nc: 5  # 类别数（可根据需要保留或删除）
names: ['fire', 'smoke','leafs','sewage','tissuse']
